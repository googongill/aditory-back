server {
    listen 80;
    listen [::]:80;

    server_name api.aditory.site;
    access_log off;

    location /.well-known/acme-challenge/ {
            allow all;
            root /var/www/certbot;
    }
}